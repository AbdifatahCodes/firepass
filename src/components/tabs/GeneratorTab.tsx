// GeneratorTab.tsx
import TabsHeader from "~components/TabsHeader";

const GeneratorTab: React.FC = () => {
    // Content for the Generator tab
    return (
      <div>
        <TabsHeader isTitle={true} titleName="Generator" />
      </div>
    );
};

export default GeneratorTab;